import { exp } from "react-native-reanimated";
import { Expenses } from "../data/model";

// const groupedByYear = (expense: Expenses[]) => {
//   return expense.reduce((result, transaction) => {
//     const year = transaction.date.getFullYear();
//     if (!result[year]) {
//       result[year] = [];
//     }
//     result[year].push(transaction);
//     return result;
//   }, {});
// };
